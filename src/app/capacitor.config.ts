import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.trinhnam12345z.cameragallery',
  appName: 'CameraGallery',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
